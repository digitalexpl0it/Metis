[package]
name = "metis-secrets"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Credential storage for Metis via the freedesktop Secret Service (oo7)"

[dependencies]
oo7 = { version = "0.6.0", default-features = false, features = ["tokio", "native_crypto"] }
